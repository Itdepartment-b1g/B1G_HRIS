import ComingSoon from '@/components/ComingSoon';
import { Calendar } from 'lucide-react';

const Leave = () => (
  <ComingSoon title="Leave" description="Manage and review employee leave requests." icon={Calendar} />
);

export default Leave;
